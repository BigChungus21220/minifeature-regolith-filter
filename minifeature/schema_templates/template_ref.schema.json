{
    "properties": {
        "inherits": {
            "type": "string",
            "pattern": "^(([a-z_][a-z0-9_]*)\\.)?(<[a-z_][a-z0-9_]*>)$"
        }
    },
    "patternProperties": {
        "^\\$[a-z_][a-z0-9_]*\\$$": {}
    },
    "required": ["inherits"],
    "additionalProperties": false
}